CREATE TABLE IF NOT EXISTS asistencias (
  id        INTEGER PRIMARY KEY AUTOINCREMENT,
  nombre    TEXT,
  email     TEXT,
  cantidad  INTEGER,
  asistencia TEXT,
  equipo    TEXT,
  fecha     TEXT
);
